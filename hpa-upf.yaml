apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: open5gs-upf
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: open5gs-upf
  minReplicas: 1
  maxReplicas: 5
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 10
